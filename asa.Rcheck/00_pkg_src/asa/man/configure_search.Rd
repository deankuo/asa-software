% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{configure_search}
\alias{configure_search}
\title{Configure Python Search Parameters}
\usage{
configure_search(
  max_results = NULL,
  timeout = NULL,
  max_retries = NULL,
  retry_delay = NULL,
  backoff_multiplier = NULL,
  captcha_backoff_base = NULL,
  page_load_wait = NULL,
  conda_env = "asa_env"
)
}
\arguments{
\item{max_results}{Maximum number of search results to return (default: 10)}

\item{timeout}{HTTP request timeout in seconds (default: 15)}

\item{max_retries}{Maximum retry attempts on failure (default: 3)}

\item{retry_delay}{Initial delay between retries in seconds (default: 2)}

\item{backoff_multiplier}{Multiplier for exponential backoff (default: 1.5)}

\item{captcha_backoff_base}{Base multiplier for CAPTCHA backoff (default: 3)}

\item{page_load_wait}{Wait time after page load in seconds (default: 2)}

\item{conda_env}{Name of the conda environment (default: "asa_env")}
}
\value{
Invisibly returns a list with the current configuration
}
\description{
Sets global configuration values for the Python search module.
These values control timeouts, retry behavior, and result limits.
}
\examples{
\dontrun{
# Increase timeout for slow connections
configure_search(timeout = 30, max_retries = 5)

# Get more results
configure_search(max_results = 20)
}

}
